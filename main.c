#include <stdio.h>
#include "pgsm.h"

int main(void) {
    Player player = {
        .coins = 1000,
        .waters = 50,
        .nutrients = 20,
        .day = 1,
        .plot_count = 2
    };
    Plot plots[2];
}